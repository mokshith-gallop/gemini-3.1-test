CREATE SCHEMA IF NOT EXISTS `${DS_GOLD_ANALYTICS}`;
CREATE TABLE IF NOT EXISTS `${DS_GOLD_ANALYTICS}`.dmbillingheader (
  billingheaderid INT64,
  patientid INT64,
  primarypayercode STRING,
  secondarypayercode STRING,
  tertiarypayercode STRING,
  financialclasscode STRING,
  organizationid INT64,
  facilityid INT64,
  operatingproviderid INT64,
  attendingproviderid STRING,
  otherproviderid STRING,
  filefrequency STRING,
  filetype STRING,
  recordreceiveddate DATETIME,
  placementdate DATETIME,
  hicnumber STRING,
  accountnumber STRING,
  mrn STRING,
  billtypecode STRING,
  facilitytypecode STRING,
  clasificationcode STRING,
  principaldiagnosiscode STRING,
  admittingdiagnosiscode STRING,
  proceduremodifier1 STRING,
  proceduremodifier2 STRING,
  proceduremodifier3 STRING,
  proceduremodifier4 STRING,
  patientstatuscode STRING,
  diagnosisdrgcode STRING,
  typeofservicecode STRING,
  placeofservice STRING,
  adjustmenttypecode STRING,
  admissiontypecode STRING,
  admissionsourcecode STRING,
  servicefromdate DATETIME,
  servicethroughdate DATETIME,
  admitdate DATETIME,
  dischargedate DATETIME,
  effectivedate DATETIME,
  billdate DATETIME,
  totalchargeamount NUMERIC,
  totalpayments NUMERIC,
  totaladjustments NUMERIC,
  encounterid INT64,
  createdby STRING,
  createddate DATETIME,
  modifiedby STRING,
  modifieddate DATETIME,
  recordstartdate DATETIME,
  recordenddate DATETIME,
  billingarea STRING,
  department STRING,
  division STRING,
  speciality STRING,
  servicearea STRING,
  region STRING,
  referringprovidername STRING,
  attendingprovidername STRING,
  provision STRING,
  tag STRING,
  allinclusivecharges NUMERIC,
  ancillarycharges NUMERIC,
  lineitemvisitcharges NUMERIC,
  eligibilitstatus STRING,
  arbalance NUMERIC,
  patientname STRING,
  rconumber STRING,
  reasonforunbilled STRING,
  dxcode STRING,
  batchnum STRING,
  organizationgroupid STRING,
  batch_id STRING
)
PARTITION BY DATE(recordreceiveddate)
CLUSTER BY primarypayercode
OPTIONS (
  description='Migrated from shc_datamart.dmbillingheader'
);
